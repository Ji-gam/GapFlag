# SESSION_HANDOFF.md — 다음 세션 인수인계

v1.6 · 작성 2026-08-16, 갱신 2026-09-01 → 2026-09-04(v1.2 → ... → v1.5 → v1.6) · 이 문서 하나만 읽으면 이전 대화 없이 이어서 작업할 수 있다.

**v1.6에서 바뀐 것**: **`data/seed_compounds.csv` 34건(35행)이 확정됐다(OI-01 해소)** —
5차례 핸드오프(v1.1~v1.5) 동안 "초안, 팀 검수 전"으로 남아 있던 항목이다. 코드·CSV 변경은
없음. v1.5의 매트릭스 기준선 상대화(PR #27) 이후 재검증한 결과, 이 34건이 4사분면에
고르게 분산되고(핵심 9 / 우선검토 9 / 회피 9 / 경쟁 8) 중복·커버리지 문제가 없어 그대로
확정했다. §0·§2-4(신설)·§7·§8을 갱신. 나머지는 v1.5와 동일 —
전체 배경은 `docs/SESSION_HANDOFF_5.md` 참고.

---

## 0. 30초 요약

| 항목 | 내용 |
|---|---|
| 프로젝트 | **Gap & Flag (갭앤플래그)** — 동물용 의약품 타깃·성분 스크리닝 도구 |
| 소속 | 2026 KU AI Nest AI 창업 실전 프로그램 · 바이오 트랙 |
| 기간 | 2026.8.22 ~ 9.6 (발표 임박, 2026-09-04 기준) |
| 저장소 | `D:\Project\Ku_nest\GapFlag`, GitHub `Ji-gam/GapFlag` |
| 현재 단계 | 화면 4개 동작, 수집 6종 실동작, 매트릭스 기준선 상대화 완료(v1.5). **대상 성분
34건 확정(v1.6, OI-01 해소)** — 더 이상 "초안" 아님. S3(비교)·R4(EPO)만 남음 |

---

## 1~6. (v1.5와 동일)

`docs/SESSION_HANDOFF_5.md` §1~§6 그대로 유효. 코드·산식·역할분담·산출물 변경 없음.

---

## 2-4(신설). `data/seed_compounds.csv` 확정 근거

확정 전 검증한 것(2026-09-04):

- **중복 없음** — (성분, 종) 쌍 35개 전부 유일 (propofol만 dog·cat 둘 다, 의도적)
- **종 분포** — dog 22 / cat 13
- **사분면 분포**(v1.5 기준선 적용, 재수집 결과) — 핵심(비어있는 이유가 있음) 9 / 우선검토 9 /
  회피 9 / 경쟁구간 8. NULL(지수 계산 불가) 0건
- **커버리지** — 전 행 위험지수 75%(R1~R3, R4만 없음) / 기회지수 100%(O1~O3 전부)
- **구성 카테고리** — 종간대사 사례 6 · 수의승인 경쟁구간 14 · 항체 3 · 항체 유사 신약 4 ·
  인체약 전용 확장 8 (note 컬럼 기준)

이 중 어느 것도 팀 재검토가 필요한 이상 신호가 없어 **그대로 확정**했다(대표 확인 완료,
2026-09-04). 앞으로 이 파일을 바꾸려면 위 검증을 다시 통과해야 한다.

---

## 7. 다음에 할 일 (우선순위 순, v1.5 대비 1번 항목 해소돼 빠짐)

### 즉시

1. **발표 스크립트에 "상대 순위" 설명 포함** (v1.5 §7-3과 동일, 여전히 유효)
2. 팀 4명 전원 `uv sync` + `uvicorn` 실행 재확인 — 특히 확정된 seed로 재수집됐는지

### 시간이 남으면

3. R4(EPO 특허) — 키 승인 대기 중
4. S3(후보 비교) 화면 — P1

### 검증이 필요한 것

v1.4 §7의 6~8번과 동일(인터뷰, Green Book sponsor 파싱, 공공데이터포털 파싱). 발표 이후 과제.

---

## 8. 미결정 사항

v1.5 §8에서 **OI-01(대상 성분 최종 목록)이 해소됨**. 나머지(`docs/SESSION_HANDOFF_3.md` §8)는
그대로:

| # | 항목 | 상태 |
|---|---|---|
| OI-01 | 대상 성분 최종 목록 | ✅ 해소 (2026-09-04, 이 문서) |
| OI-02 | 항체·단백질 의약품 우선 여부 확정 | 미결 — 34건에 항체 3·항체 유사 4건 포함돼 사실상 반영됨 |
| OI-03 | 배포처 결정 및 빈 앱 배포 | 미결 |
| OI-04 | 성분명 동의어 사전 초안 | 미결(MVP 범위 밖) |
| OI-05 | §4-2 정규화 계수 실측 조정 | v1.5의 기준선 상대화로 대체됨 — 계수 자체 조정 불필요해짐 |
| OI-06 | 사분면 라벨 한글 최종 문구 | 미결 — "임계값→기준선" 문구 노출 방식 재검토 필요(v1.5) |
| OI-07 | 전북 클러스터·한국동물약품협회 접촉 여부 | 미결 |

---

## 9. 다음 세션 시작 프롬프트 예시

```
Gap & Flag 프로젝트를 이어서 진행한다.
D:\Project\Ku_nest\GapFlag 폴더가 연결되어 있고,
docs/SESSION_HANDOFF_6.md 를 먼저 읽어라. git log와 origin 대비 로컬 상태도 확인해라.

오늘 할 일: [예) S3 비교 화면 착수]
```

나머지 주의사항은 v1.5 §9와 동일.

---

## 10. 발표·심사 대비 메모

v1.5와 동일. `docs/SESSION_HANDOFF_5.md` §10 참고.
